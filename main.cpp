/*
 * pre-process（预处理）：发生在真正编译之前
 * # 开头为预处理指令，编译器做的第一件事是预处理所有预处理指令
 *
 * delcaration（声明）：创建变量并定义其类型的语句
 * definaition（定义）：
 */
#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
