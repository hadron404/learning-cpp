#include  <iostream>
#include <array>


void cocktailShakerSort(std::array<int,10>  , int length) {
    int left = 0, right = length - 1;


    // 变量 variable
    // 赋值 assign
    // 迭代
    // 条件判断
    // while(left < right)
}

int main() {
    int array[5] = {4, 2, 7, 1, 3};

}
